query search($term: String!) {
  search(term: $term) {
    id
    volumeInfo {
      title
      description
      averageRating
      ratingsCount
      authors
      imageLinks {
        smallThumbnail
        thumbnail
        small
        medium
        large
        extraLarge
      }
      pageCount
      publisher
      publishedDate
      mainCategory
      categories
    }
  }
}
